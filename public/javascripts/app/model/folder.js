define(function(require) {
    "use strict";

    function Folder() {
        this.id = null;
        this.name = null;
    }

    (function () {
        this.load = function () {

        };

        this.deleteFolder = function (id) {

        };

        this.createFolder = function () {

        };
    }).call(Folder.prototype);

    return Folder;
});